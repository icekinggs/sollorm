FROM node:22-alpine

WORKDIR /app

# Instala dependências primeiro (melhor cache)
COPY package.json ./
RUN npm install

# Código será montado via volume em dev (live reload)
EXPOSE 5173

CMD ["npm", "run", "dev"]
